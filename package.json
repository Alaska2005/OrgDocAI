{
  "name": "orgdoc-ai",
  "version": "1.0.0",
  "description": "OrgDoc AI monorepo",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}